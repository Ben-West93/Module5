# exercises/security-basics/requirements.txt
#
# Install with:  pip install -r requirements.txt

fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6

# --- L10 ---
#
# Nothing new for the app itself. CORSMiddleware ships with Starlette, the
# rate limiter is a dict and time.monotonic(), and the sanitizer is `re`.
#
# httpx is here for verify_security.py: it is the transport under TestClient,
# and the script also uses it directly against a live uvicorn server to test
# the rate limiter under genuinely concurrent requests.
httpx>=0.27
