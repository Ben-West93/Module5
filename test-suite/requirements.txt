# exercises/test-suite/requirements.txt
#
# Install with:  pip install -r requirements.txt

fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6

# --- L10 ---
#
# Nothing new for the app itself. CORSMiddleware ships with Starlette, the
# rate limiter is a dict and time.monotonic(), and the sanitizer is `re`.
#
# httpx is the transport under TestClient, and verify_security.py and
# demo_rate_limit.py also use it directly against a live uvicorn server.
httpx>=0.27

# --- L11 ---
#
# The test runner for tests/.
pytest>=8.0
