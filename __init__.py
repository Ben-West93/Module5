"""
The `app` package.

This file is what makes `app` importable as a Python package, which is why
`from app.routers import recipes` works and why the server is started with
`uvicorn app.main:app` rather than `uvicorn main:app`.

It can stay empty — the file existing is the point.
"""

